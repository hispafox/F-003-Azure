> **Versión:** v2 | **Módulo:** 9 | **Sub:** 9.4 | **Slides:** 18 | **Fecha:** 2026-04-22 | **Estado:** ✅

# Submódulo 9.4 — MCP: Model Context Protocol — IA conectada con herramientas

## Slide 1 — Portada
**Módulo 9 · Submódulo 9.4**
MCP: Model Context Protocol
Conectar Claude Code con Azure DevOps, GitHub, bases de datos y APIs

---

## Slide 2 — Qué es MCP y por qué importa
MCP (Model Context Protocol) es un protocolo abierto que permite a la IA conectarse con herramientas externas. En vez de copiar-pegar datos entre herramientas, Claude Code puede leer de Azure DevOps, escribir en GitHub, consultar bases de datos y llamar APIs — todo desde el terminal.

```
Sin MCP:
  Desarrollador copia work item de ADO → pega en Claude → copia respuesta → pega en código

Con MCP:
  Claude Code lee work item de ADO → genera código → crea PR → actualiza work item
  Todo en una conversación, sin cambiar de herramienta.
```

---

## Slide 3 — Configurar MCP servers

```json
// ~/.config/claude/claude_desktop_config.json
{
  "mcpServers": {
    "azure-devops": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-azure-devops"],
      "env": {
        "AZURE_DEVOPS_ORG": "https://dev.azure.com/mi-org",
        "AZURE_DEVOPS_PAT": "xxx"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_TOKEN": "ghp_xxx" }
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/dev/projects"]
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"],
      "env": { "DATABASE_URL": "postgresql://..." }
    }
  }
}
```

Cada MCP server expone "tools" que Claude Code puede invocar. Azure DevOps expone: list_work_items, create_work_item, list_pull_requests, etc.

---

## Slide 4 — MCP + Azure DevOps: gestión de trabajo

```bash
claude

> Muéstrame los bugs abiertos del sprint actual con prioridad ≤ 2

# Claude Code invoca: azure-devops.list_work_items(query="SELECT ... WHERE ...")
# Resultado: tabla con ID, título, prioridad, asignado

> Crea una user story: "Como usuario, quiero filtrar pedidos por fecha"
  Asígnala a Pedro, sprint 15, prioridad 2, con estos acceptance criteria:
  - Endpoint GET /api/pedidos?desde=2025-01-01&hasta=2025-06-30
  - Paginación con skip/take
  - Debe funcionar con Cosmos DB partition key

# Claude Code invoca: azure-devops.create_work_item(...)

> Ahora implementa esa story en el código

# Claude Code lee el work item, genera el código, y puede crear un PR
```

---

## Slide 5 — MCP + GitHub: código y PRs

```bash
> Lista los PRs abiertos del repo ventas-api

# Claude Code invoca: github.list_pull_requests(repo="mi-org/ventas-api")

> Revisa el PR #42 y dame un resumen de los cambios

# Claude Code invoca: github.get_pull_request(repo="mi-org/ventas-api", number=42)
# Lee los diffs, analiza los cambios, y genera un resumen

> Crea un PR con los cambios que hemos hecho:
  Título: "feat: implementar filtro de pedidos por fecha"
  Descripción: [generada automáticamente desde los cambios]
  Branch: feature/filtro-pedidos → main

# Claude Code invoca: github.create_pull_request(...)
```

---

## Slide 6 — MCP + Bases de datos

```bash
> Muéstrame los 10 pedidos más recientes de Cosmos DB

# Claude Code invoca: cosmos.query("SELECT TOP 10 * FROM c ORDER BY c._ts DESC")

> ¿Cuántos pedidos se crearon hoy por hora?

# Claude Code invoca: cosmos.query("SELECT DateTimePart('hh', c.creado) as hora, COUNT(1) as total FROM c WHERE c.creado >= '2025-06-15' GROUP BY DateTimePart('hh', c.creado)")

> Compara el volumen de pedidos de esta semana con la anterior

# Claude Code ejecuta queries comparativas y genera un resumen
```

---

## Slide 7 — MCP + Notion/Slack/Email

```bash
# Notion: documentación del equipo
> Actualiza la página de Notion "Arquitectura" con el diagrama que acabamos de crear

# Slack: comunicación
> Envía un mensaje al canal #dev-ventas: "Pipeline v2.4.150 desplegado a producción"

# Email (con Gmail MCP server)
> Busca en mi email la última factura de Azure y muéstrame el total
```

MCP es extensible: cualquier servicio con API puede tener un MCP server. La comunidad crea nuevos servers continuamente.

---

## Slide 8 — Crear un MCP server custom

Si necesitáis conectar Claude Code con una API interna:

```typescript
// mcp-server-ventas.ts
import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

const server = new Server({ name: "ventas-api", version: "1.0.0" }, {
  capabilities: { tools: {} }
});

server.setRequestHandler("tools/list", async () => ({
  tools: [{
    name: "get_pedido",
    description: "Obtener un pedido por ID",
    inputSchema: { type: "object", properties: { id: { type: "string" } }, required: ["id"] }
  }, {
    name: "list_pedidos_hoy",
    description: "Listar pedidos creados hoy",
    inputSchema: { type: "object", properties: {} }
  }]
}));

server.setRequestHandler("tools/call", async (request) => {
  if (request.params.name === "get_pedido") {
    const response = await fetch(`https://api-ventas.azurewebsites.net/api/pedidos/${request.params.arguments.id}`);
    return { content: [{ type: "text", text: await response.text() }] };
  }
});

const transport = new StdioServerTransport();
await server.connect(transport);
```

---

## Slide 9 — Seguridad con MCP

**Riesgos:**
- MCP servers tienen acceso a servicios reales (ADO, GitHub, bases de datos)
- Un prompt malicioso podría intentar que Claude ejecute acciones destructivas
- Las credenciales (PATs, tokens) están en la config

**Mitigaciones:**
- Usar tokens con permisos mínimos (read-only donde sea posible)
- Claude Code pide confirmación antes de ejecutar acciones de escritura
- Revisar siempre las acciones que Claude propone antes de confirmar
- No compartir la config de MCP (contiene credenciales)
- Rotar tokens regularmente

```json
// Token de ADO con permisos mínimos
// Scope: Work Items (Read), Code (Read), Build (Read)
// NO: Code (Write), Build (Execute), Project Admin
```

---

## Slide 10 — Flujo de trabajo completo con MCP

```bash
claude

> Buenos días. ¿Qué tengo asignado para hoy?
# → Lee work items de ADO asignados a mí

> Empecemos con el bug #1567 "Login falla en VPN"
# → Lee detalles del bug, incluidos pasos para reproducir

> Analiza el código de AuthService.cs y encuentra la causa del bug
# → Lee el archivo, identifica el problema

> Genera el fix y los tests
# → Edita AuthService.cs, crea AuthServiceTests.cs

> Ejecuta los tests
# → Ejecuta dotnet test, verifica que pasan

> Crea un PR con los cambios
# → Crea PR en GitHub/ADO: "fix: resolver login VPN timeout (#1567)"

> Actualiza el bug en ADO como "In Review"
# → Actualiza work item estado + link al PR
```

Todo sin salir del terminal. 20 minutos para un flujo que normalmente tarda 1 hora entre cambio de herramientas.

---

## Slide 11 — MCP servers disponibles (2026)

| Server | Para qué | Oficial |
|---|---|---|
| filesystem | Leer/escribir archivos | ✓ Anthropic |
| github | Issues, PRs, repos | ✓ Anthropic |
| azure-devops | Work items, PRs, builds | Community |
| postgres | Queries SQL | ✓ Anthropic |
| sqlite | Base de datos local | ✓ Anthropic |
| notion | Páginas y bases de datos | Community |
| slack | Mensajes y canales | Community |
| google-drive | Documentos | Community |
| azure-cosmos | Queries Cosmos DB | Community |
| puppeteer | Browser automation | ✓ Anthropic |
| brave-search | Búsqueda web | ✓ Anthropic |

---

## Slide 12 — Cierre del submódulo
MCP transforma Claude Code de "IA que genera texto" a "IA que opera herramientas reales". Leer work items, crear PRs, consultar bases de datos, enviar mensajes — todo desde una conversación en el terminal. La productividad del desarrollador se multiplica.

Siguiente: buenas prácticas y limitaciones de IA en desarrollo.

---

## Slide 13 — MCP: flujo completo de desarrollo

```bash
claude

# Mañana de trabajo con MCP:
> ¿Qué bugs críticos tengo asignados?
# → ADO: lista 2 bugs P1

> Dame los detalles del bug #1892
# → ADO: bug con pasos para reproducir

> Revisa el código de PaymentService y encuentra la causa
# → Filesystem: lee PaymentService.cs, identifica race condition

> Genera el fix
# → Edita PaymentService.cs con lock + async pattern

> Escribe un test que reproduzca el bug
# → Crea PaymentServiceTests.cs

> Ejecuta los tests
# → Bash: dotnet test → 15/15 passed

> Crea un PR
# → GitHub/ADO: PR "fix: race condition in PaymentService (#1892)"

> Mueve el bug a "In Review" y linkea el PR
# → ADO: actualiza work item

# Todo sin salir del terminal. 25 minutos.
```

---

## Slide 14 — MCP: crear dashboards y reportes

```bash
> Genera un resumen semanal para el standup:
  - Work items completados esta semana (de ADO)
  - PRs mergeados (de GitHub/ADO)
  - Builds fallidos (de ADO Pipelines)
  - Errores más frecuentes (de App Insights)
  - Formato: Markdown para pegar en Teams
```

MCP con múltiples servers: ADO para work items, GitHub para PRs, App Insights API para errores. Claude Code combina las fuentes y genera el reporte.

---

## Slide 15 — MCP: automatización con scripts

```python
# Script Python que usa Claude Code API + MCP para automatización
import subprocess

# Cada mañana: generar daily briefing
result = subprocess.run([
    'claude', '-p', 
    'Genera mi daily briefing: work items asignados, PRs pendientes de review, alertas activas',
    '--no-interactive'
], capture_output=True, text=True)

# Enviar por email o Slack
send_to_slack(result.stdout)
```

---

## Slide 16 — Cierre del submódulo (v2)
MCP es el puente entre la IA y vuestras herramientas reales. En vez de copiar-pegar entre Azure DevOps, GitHub, bases de datos y el terminal, Claude Code opera directamente sobre las herramientas. El resultado: flujos de trabajo que tardan una hora se completan en 15 minutos.

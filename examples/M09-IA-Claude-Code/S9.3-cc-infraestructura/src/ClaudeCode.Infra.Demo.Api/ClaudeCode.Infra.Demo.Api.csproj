<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- S9.3 Claude Code para infraestructura. Conceptual (lección 9):
       Claude Code genera Bicep/YAML/Dockerfile; aquí extraemos las
       heurísticas pedagógicas:
         - parser de requisitos IaC (lista recursos, region, slots,
           compliance, security knobs detectados)
         - generador de prompts canónicos para los 7 escenarios IaC
           más comunes (Bicep, Dockerfile, GHA pipeline, reverse
           ARM→Bicep, runbook, ops script, audit security)
         - audit checker (HTTPS, MI, tags, public access, TLS) que
           reporta los findings de slide 15
       Sin PackageReference externas. -->

  <ItemGroup>
    <InternalsVisibleTo Include="ClaudeCode.Infra.Demo.Api.Tests" />
  </ItemGroup>

</Project>

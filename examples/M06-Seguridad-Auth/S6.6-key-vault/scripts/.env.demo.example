# Copia este archivo a `.env.demo` y rellena con tus datos
# (.env.demo esta ignorado por git -- no se sube)
#
# Scripts SOLO LECTURA (inventario de Key Vault — nunca valores de
# secretos). Sin coste ni cleanup.

SUBSCRIPTION_ID=00000000-0000-0000-0000-000000000000

# Opcional: limitar a un Key Vault concreto (nombre).
KV_NAME=

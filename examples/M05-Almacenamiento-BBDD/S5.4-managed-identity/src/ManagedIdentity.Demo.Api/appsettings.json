{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "// StorageBlobEndpoint": "https://<cuenta>.blob.core.windows.net — SOLO la URL, sin key (slide 6/8). Vacío → la demo /blob/contenedores responde 503.",
  "StorageBlobEndpoint": "",

  "// Azure": "Opcionales: UserAssignedClientId (slide 22), TenantId (slide 25). LocalDev=true acelera en local (slide 16).",
  "Azure": {
    "UserAssignedClientId": "",
    "TenantId": "",
    "LocalDev": false
  },

  "// Conexiones": "Las escanea /seguridad/checklist (slide 13). En producción TODAS deben ser sin secreto.",
  "Conexiones": {
    "Sql": "Server=tcp:sql.database.windows.net,1433;Database=db;Authentication=Active Directory Default;Encrypt=true;",
    "Cosmos": "AccountEndpoint=https://acct.documents.azure.com:443/",
    "Blob": "https://acct.blob.core.windows.net",
    "ApiExterna": "@Microsoft.KeyVault(VaultName=kv-prod;SecretName=api-key)"
  }
}
